# Environment variables
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOCAL_IP=193.246.162.61

# Database - используем SQLite для простоты разработки
DB_TYPE=sqlite
DB_PATH=./database.sqlite

# JWT Secret
JWT_SECRET=freedom_group_super_secret_key_2026_development

# Session timeout (30 minutes)
SESSION_TIMEOUT=30m

# SMTP Settings (отключены для разработки)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

# Telegram Bot (отключен для разработки)
TELEGRAM_BOT_TOKEN=

# Admin credentials (default admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Frontend URL
FRONTEND_URL=http://localhost:8081DB_TYPE=sqlite
