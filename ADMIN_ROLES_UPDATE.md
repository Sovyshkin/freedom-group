# Инструкция по обновлению системы с поддержкой ролей администраторов

## Изменения в системе

Добавлена поддержка двух ролей администраторов:
- **Суперадминистратор** - полный доступ ко всем функциям, включая управление другими админами
- **Администратор** - стандартный доступ к управлению партнерами и документами

## Шаги для обновления на сервере

### 1. Остановите backend (если запущен)

```bash
cd /path/to/freedom-group/backend
pm2 stop backend
```

### 2. Обновите код

```bash
cd /path/to/freedom-group
git pull origin main
```

### 3. Запустите миграцию базы данных

```bash
cd backend
npm run migrate-admin-role
```

Эта команда добавит поле `role` в таблицу `admin` и установит роль `superadmin` для пользователя, указанного в `ADMIN_USERNAME` в файле `.env`.

### 4. Убедитесь, что в .env файле указаны правильные данные суперадмина

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=X19Kqrm}8o8SaKJrn
```

### 5. Пересоберите frontend (если нужно)

```bash
cd ../frontend
npm run build
```

### 6. Запустите backend

```bash
cd ../backend
pm2 start backend
# или
npm start
```

### 7. Проверьте логи

```bash
pm2 logs backend
```

Должно появиться:
```
✅ Telegram бот инициализирован с polling
✅ SMTP сервер готов для отправки писем
🚀 Сервер запущен на 0.0.0.0:3001
```

## Использование

### Вход в систему

Войдите под учетными данными из `.env` файла:
- **Логин**: значение `ADMIN_USERNAME`
- **Пароль**: значение `ADMIN_PASSWORD`

После входа у суперадмина появится дополнительная вкладка **"Администраторы"**.

### Управление администраторами

В разделе "Администраторы" суперадмин может:
1. **Создавать новых админов** - нажмите кнопку "Добавить администратора"
2. **Менять пароли админов** - нажмите на иконку редактирования
3. **Удалять админов** - нажмите на иконку удаления (нельзя удалить суперадмина и себя самого)

### API Endpoints (для суперадмина)

- `GET /api/admin/admins` - получить список всех админов
- `POST /api/admin/admins` - создать нового админа
- `PUT /api/admin/admins/:id/password` - изменить пароль админа
- `DELETE /api/admin/admins/:id` - удалить админа

## Безопасность

1. **Пароль в .env**: Символ `}` в пароле обрабатывается корректно, кавычки не обязательны
2. **Суперадмина нельзя удалить** - защита на уровне БД
3. **Нельзя удалить самого себя** - проверка на уровне API
4. **Минимум 6 символов** для всех паролей

## Откат изменений (если что-то пошло не так)

Если нужно откатить миграцию:

```bash
cd backend
sqlite3 database.sqlite
```

Затем в SQLite консоли:

```sql
-- Посмотреть текущие колонки
PRAGMA table_info(admin);

-- Откатить (удалить колонку role)
-- В SQLite нельзя просто удалить колонку, нужно пересоздать таблицу
-- Проще восстановить из backup или пересоздать БД через npm run init-db
```

## Структура файлов

### Backend

- `backend/src/models/sqlite-database.js` - обновлена схема таблицы admin
- `backend/src/middleware/auth.js` - добавлен middleware `verifySuperAdmin`
- `backend/src/routes/admin.js` - добавлены endpoints для управления админами
- `backend/src/routes/auth.js` - обновлен login для возврата роли
- `backend/scripts/migrate-add-admin-role.js` - скрипт миграции
- `backend/scripts/initDb.js` - обновлен для создания суперадмина

### Frontend

- `frontend/src/views/AdminDashboard.vue` - добавлена вкладка "Администраторы"
- `frontend/src/stores/auth.js` - уже корректно сохраняет роль из токена

## Troubleshooting

### Ошибка "колонка role уже существует"

Миграция уже была выполнена. Это нормально, повторный запуск безопасен.

### Не вижу вкладку "Администраторы"

1. Проверьте, что вы вошли под суперадмином
2. Откройте DevTools → Console → проверьте `authStore.user.adminRole`
3. Должно быть `"superadmin"`

### Не могу создать админа

1. Проверьте логи backend: `pm2 logs backend`
2. Убедитесь, что backend перезапущен после миграции
3. Проверьте права доступа (только суперадмин может управлять админами)

## Контакты

При возникновении проблем проверьте логи:
```bash
pm2 logs backend --lines 50
```
