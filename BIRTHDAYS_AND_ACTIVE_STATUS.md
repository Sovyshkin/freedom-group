# Дни рождения и статус активности партнёров

## Обзор

Добавлена функциональность отслеживания дней рождения партнёров и управления их активным статусом.

## Новые поля в таблице partner

### 1. birthDate (DATE)
- **Тип**: DATE (опционально)
- **Описание**: Дата рождения партнёра
- **Использование**: Для отправки уведомлений о днях рождения

### 2. active (INTEGER)
- **Тип**: INTEGER (0 или 1)
- **Значение по умолчанию**: 1 (активный)
- **Описание**: Статус активности партнёра
- **Функциональность**:
  - **active = 0**: Партнёр не может войти в систему и не получает уведомления
  - **active = 1**: Партнёр активен, имеет полный доступ

## Уведомления о днях рождения

### Получатель уведомлений
- **Telegram ID**: 1225102536
- **Пользователь**: @AndreyBilevich

### Расписание проверки
- **Время**: Каждый день в 9:00 (по серверному времени)
- **Механизм**: Cron задача (node-cron)

### Формат уведомления
```
🎂 Дни рождения сегодня!

🎉 Имя Партнёра 1
🎉 Имя Партнёра 2

Всего именинников: 2
```

### Условия отправки
- Проверяются только **активные** партнёры (active = 1)
- Сравнивается месяц и день рождения с текущей датой
- Если именинников нет, уведомление не отправляется

## Контроль доступа по статусу active

### Вход в систему
При попытке входа неактивного партнёра:
```json
{
  "success": false,
  "message": "Партнер неактивен"
}
```

### Фильтрация уведомлений
При публикации документов:
- Email уведомления отправляются только активным партнёрам
- Telegram уведомления отправляются только активным партнёрам
- Неактивные партнёры пропускаются с логом: `⏭️ Пропуск уведомлений для неактивного партнёра [Имя]`

## Использование в API

### Создание партнёра
```javascript
POST /api/admin/partners
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "telegram": "123456789",
  "alias": "ivan",
  "password": "password123",
  "birthDate": "1990-05-15",  // опционально
  "active": true              // опционально, по умолчанию true
}
```

### Обновление партнёра
```javascript
PUT /api/admin/partners/:id
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "telegram": "123456789",
  "alias": "ivan",
  "birthDate": "1990-05-15",  // опционально
  "active": false             // деактивация партнёра
}
```

### Получение списка партнёров
```javascript
GET /api/admin/partners

Response:
{
  "success": true,
  "data": [
    {
      "Inc": 1,
      "Name": "Иван Иванов",
      "Email": "ivan@example.com",
      "Telegram": "123456789",
      "BirthDate": "1990-05-15",
      "Active": 1,
      "CreatedAt": "2024-01-15T10:30:00.000Z",
      ...
    }
  ]
}
```

## UI изменения

### Форма создания партнёра
Добавлены поля:
1. **Дата рождения** - date input (опционально)
2. **Активный партнёр** - checkbox с подсказкой

### Форма редактирования партнёра
Те же поля доступны для редактирования:
- Дата рождения может быть изменена
- Статус активности может быть изменён

### Подсказка для администраторов
> Неактивные партнёры не могут войти в систему и не получают уведомления

## Миграция базы данных

### Выполнение миграции
```bash
npm run migrate-partner-fields
```

### Что делает миграция
1. Добавляет колонку `birthDate` (DATE, NULL) если её нет
2. Добавляет колонку `active` (INTEGER, DEFAULT 1) если её нет
3. Устанавливает `active = 1` для всех существующих партнёров
4. Безопасна для повторного запуска (проверяет наличие колонок)

## Сервисы

### birthdayService.js
**Методы**:
- `checkBirthdays()` - Проверяет дни рождения и отправляет уведомление
- `getAllBirthdays()` - Возвращает список всех партнёров с днями рождения

**Константы**:
- `BIRTHDAY_NOTIFICATION_CHAT_ID` = '1225102536'

## Логирование

### Проверка дней рождения
- `🎂 Запуск проверки дней рождения...` - Начало проверки
- `🎂 Сегодня нет именинников среди активных партнёров` - Нет именинников
- `🎂 Уведомление о X именинниках отправлено в Telegram (ID: 1225102536)` - Успешная отправка

### Публикация документов
- `⏭️ Пропуск уведомлений для неактивного партнёра [Имя]` - Партнёр неактивен

## Примеры использования

### Деактивация партнёра
1. Открыть список партнёров в админ-панели
2. Нажать "Редактировать" на нужном партнёре
3. Снять галочку "Активный партнёр"
4. Сохранить изменения
5. Партнёр больше не сможет войти и не будет получать уведомления

### Добавление дня рождения
1. При создании или редактировании партнёра
2. Выбрать дату в поле "Дата рождения"
3. Сохранить
4. В день рождения партнёра Андрей получит уведомление в Telegram в 9:00

## Технические детали

### SQL запрос для поиска именинников
```sql
SELECT name, birthDate
FROM partner
WHERE strftime('%m-%d', birthDate) = strftime('%m-%d', 'now')
  AND active = 1
ORDER BY name
```

### Проверка активности в middleware
```javascript
// В auth.js verifyPartner
if (!partner || !partner.IsActive) {
  return res.status(403).json({ 
    success: false, 
    message: 'Партнер неактивен' 
  });
}
```

## Обратная совместимость

- Все существующие партнёры получают `active = 1` при миграции
- Поле `birthDate` остаётся NULL если не заполнено
- API поддерживает запросы без новых полей (они опциональны)
- Frontend отображает корректную информацию даже если поля не заполнены

---

**Дата создания**: 2024  
**Версия**: 1.0.0
